"""
Prepare Layer

This layer initializes an experiment workspace from an external result.json-like artifact:
- Create workspace folders (repos/, dataset_candidate/)
- Materialize idea.md (markdown proposal) for downstream Code/Science layers
"""
